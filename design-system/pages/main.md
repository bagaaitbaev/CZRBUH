# Main Page Design Rules

- Keep all existing behavior and data rendering.
- Apply the master design system only to `index.html` and `styles.css`.
- Preserve dashboard density, but increase spacing and readability.
- Mobile must stack cleanly without horizontal page scroll; tables may scroll inside their wrappers.
